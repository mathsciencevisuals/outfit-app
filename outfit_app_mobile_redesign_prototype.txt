<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>fitme.ai – interactive mobile redesign</title>
  <style>
    :root {
      --bg: #f6f7fb;
      --surface: rgba(255,255,255,0.82);
      --surface-strong: #ffffff;
      --text: #151823;
      --muted: #6b7280;
      --line: #e5e7eb;
      --accent: #6d5efc;
      --accent-soft: rgba(109,94,252,0.12);
      --accent-2: #ff6fb5;
      --success: #10b981;
      --warning: #f59e0b;
      --danger: #ef4444;
      --shadow: 0 18px 40px rgba(17, 24, 39, 0.08);
      --radius-xl: 28px;
      --radius-lg: 20px;
      --radius-md: 14px;
      --phone-w: 360px;
      --phone-h: 790px;
      --page-glow: radial-gradient(circle at top left, rgba(109,94,252,0.10), transparent 28%), radial-gradient(circle at bottom right, rgba(255,111,181,0.08), transparent 24%);
    }

    * { box-sizing: border-box; }
    html, body { margin: 0; }
    body {
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--page-glow), var(--bg);
      color: var(--text);
    }

    .page {
      max-width: 1600px;
      margin: 0 auto;
      padding: 28px;
    }

    .topbar {
      display: flex;
      justify-content: space-between;
      gap: 20px;
      margin-bottom: 26px;
      flex-wrap: wrap;
    }

    .headline h1 {
      margin: 0;
      font-size: 34px;
      line-height: 1.05;
      letter-spacing: -0.04em;
    }

    .headline p {
      margin: 10px 0 0;
      max-width: 900px;
      color: var(--muted);
      font-size: 15px;
      line-height: 1.65;
    }

    .controls {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: center;
    }

    .pill-btn, .seg-btn {
      border: 1px solid var(--line);
      background: rgba(255,255,255,0.76);
      color: var(--text);
      border-radius: 999px;
      padding: 10px 14px;
      font-size: 13px;
      cursor: pointer;
      transition: all .18s ease;
    }

    .pill-btn.active, .seg-btn.active {
      background: var(--accent);
      color: white;
      border-color: var(--accent);
      box-shadow: 0 12px 24px rgba(109,94,252,0.24);
    }

    .overview {
      display: grid;
      grid-template-columns: 1.1fr 1.15fr;
      gap: 24px;
      margin-bottom: 28px;
    }

    .card {
      background: var(--surface);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255,255,255,0.65);
      box-shadow: var(--shadow);
      border-radius: 24px;
      padding: 22px;
    }

    .card h2, .card h3 {
      margin: 0 0 12px;
      letter-spacing: -0.02em;
    }

    .issue-list, .upgrade-list {
      display: grid;
      gap: 12px;
    }

    .issue, .upgrade {
      display: grid;
      grid-template-columns: 36px 1fr;
      gap: 12px;
      align-items: start;
      padding: 12px;
      border-radius: 16px;
      background: rgba(255,255,255,0.68);
      border: 1px solid var(--line);
    }

    .badge-icon {
      width: 36px; height: 36px;
      border-radius: 12px;
      display: grid; place-items: center;
      background: var(--accent-soft);
      color: var(--accent);
      font-weight: 700;
    }

    .issue b, .upgrade b { display: block; margin-bottom: 4px; font-size: 14px; }
    .issue span, .upgrade span { color: var(--muted); font-size: 13px; line-height: 1.55; }

    .phone-stage {
      display: grid;
      grid-template-columns: repeat(5, minmax(240px, 1fr));
      gap: 22px;
      align-items: start;
    }

    .phone-wrap { display: grid; gap: 10px; }
    .phone-title {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 13px;
      color: var(--muted);
      padding: 0 4px;
    }

    .phone {
      width: 100%;
      max-width: var(--phone-w);
      height: var(--phone-h);
      border-radius: 36px;
      padding: 16px;
      background: linear-gradient(180deg, rgba(255,255,255,0.76), rgba(255,255,255,0.58));
      border: 1px solid rgba(255,255,255,0.76);
      box-shadow: 0 30px 80px rgba(20,24,40,0.16);
      position: relative;
      overflow: hidden;
    }

    .phone::before {
      content: "";
      position: absolute;
      inset: 0;
      background:
        radial-gradient(circle at 80% 0%, rgba(255,255,255,0.9), transparent 24%),
        radial-gradient(circle at 0% 20%, rgba(109,94,252,0.08), transparent 24%),
        radial-gradient(circle at 100% 80%, rgba(255,111,181,0.08), transparent 26%);
      pointer-events: none;
    }

    .phone-inner {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      height: 100%;
      border-radius: 28px;
      overflow: hidden;
      background: rgba(248,250,252,0.74);
      border: 1px solid rgba(255,255,255,0.72);
      backdrop-filter: blur(10px);
    }

    .statusbar {
      height: 28px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 18px 0;
      font-size: 11px;
      color: var(--muted);
      flex-shrink: 0;
    }

    .appbar {
      padding: 14px 18px 10px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 10px;
      flex-shrink: 0;
    }

    .eyebrow { font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: .12em; }
    .title { margin-top: 4px; font-size: 22px; line-height: 1.1; font-weight: 700; letter-spacing: -0.03em; }
    .subtitle { margin-top: 5px; color: var(--muted); font-size: 13px; line-height: 1.5; }

    .circle-btn {
      width: 40px; height: 40px; border-radius: 14px; border: none; cursor: pointer;
      background: rgba(255,255,255,0.85);
      box-shadow: 0 8px 22px rgba(17,24,39,0.08);
      color: var(--text); font-size: 16px;
      transition: transform .12s ease, box-shadow .12s ease;
    }
    .circle-btn:active, .btn:active, .tab:active {
      transform: translateY(1px) scale(.98);
      box-shadow: 0 4px 12px rgba(17,24,39,0.08);
    }

    .content {
      padding: 0 16px 16px;
      display: grid;
      gap: 12px;
      overflow: auto;
      scrollbar-width: none;
    }
    .content::-webkit-scrollbar { display:none; }

    .hero {
      background: linear-gradient(135deg, var(--accent), #8b7eff 65%, var(--accent-2));
      color: white;
      border-radius: 22px;
      padding: 16px;
      box-shadow: 0 18px 30px rgba(109,94,252,0.24);
    }
    .hero h3 { margin: 0 0 6px; font-size: 17px; }
    .hero p { margin: 0; font-size: 13px; line-height: 1.5; opacity: .94; }

    .chip-row, .action-row, .stat-row, .theme-row { display:flex; gap:8px; flex-wrap: wrap; }
    .chip {
      padding: 8px 10px;
      border-radius: 999px;
      background: rgba(255,255,255,0.86);
      border: 1px solid rgba(255,255,255,0.7);
      font-size: 12px;
      color: var(--text);
    }
    .chip.tonal { background: var(--accent-soft); color: var(--accent); border-color: transparent; }
    .chip.success { background: rgba(16,185,129,0.12); color: var(--success); border-color: transparent; }
    .chip.warning { background: rgba(245,158,11,0.14); color: #b45309; border-color: transparent; }
    .chip.dark { background: rgba(17,24,39,0.84); color: white; border-color: transparent; }

    .panel {
      background: rgba(255,255,255,0.84);
      border: 1px solid var(--line);
      border-radius: 20px;
      padding: 14px;
    }
    .panel h4 { margin: 0 0 6px; font-size: 15px; letter-spacing: -0.01em; }
    .panel p { margin: 0; color: var(--muted); font-size: 13px; line-height: 1.5; }

    .metrics {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
    }
    .metric {
      padding: 12px;
      border-radius: 18px;
      background: rgba(248,250,252,0.95);
      border: 1px solid var(--line);
    }
    .metric .label { color: var(--muted); font-size: 11px; text-transform: uppercase; letter-spacing: .08em; }
    .metric .value { margin-top: 6px; font-size: 18px; font-weight: 700; letter-spacing: -0.03em; }

    .product {
      display: grid;
      grid-template-columns: 74px 1fr;
      gap: 12px;
      align-items: start;
    }
    .product-img {
      width: 74px; height: 96px; border-radius: 18px;
      background: linear-gradient(180deg, #eff3ff, #d9e2ff);
      border: 1px solid #d6dcff;
    }
    .price { font-size: 18px; font-weight: 700; letter-spacing: -0.03em; }
    .small { color: var(--muted); font-size: 12px; }

    .cta-row { display:flex; gap:10px; }
    .btn {
      border: none;
      border-radius: 16px;
      padding: 12px 14px;
      cursor: pointer;
      font-size: 13px;
      font-weight: 600;
      transition: transform .12s ease, box-shadow .12s ease;
    }
    .btn.primary { background: var(--accent); color: white; flex: 1; }
    .btn.secondary { background: rgba(255,255,255,0.92); color: var(--text); border: 1px solid var(--line); flex: 1; }
    .btn.ghost { background: transparent; color: var(--text); border: 1px dashed var(--line); }

    .camera-box {
      position: relative;
      border-radius: 24px;
      overflow: hidden;
      background: linear-gradient(180deg, #101322, #2c3454);
      min-height: 240px;
      border: 1px solid rgba(255,255,255,0.08);
    }
    video {
      width: 100%;
      height: 240px;
      object-fit: cover;
      display: block;
      background: #0f172a;
    }
    .camera-overlay {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      pointer-events: none;
    }
    .frame {
      width: 170px;
      height: 200px;
      border: 2px dashed rgba(255,255,255,0.6);
      border-radius: 28px;
      box-shadow: inset 0 0 0 999px rgba(255,255,255,0.03);
    }

    .camera-actions {
      display: grid;
      grid-template-columns: repeat(3, minmax(0,1fr));
      gap: 8px;
    }

    .preview-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }
    .preview-tile {
      border-radius: 18px;
      min-height: 116px;
      border: 1px solid var(--line);
      overflow: hidden;
      background: linear-gradient(180deg, #f8fafc, #eef2ff);
      display: grid;
      place-items: center;
      color: var(--muted);
      font-size: 12px;
      position: relative;
    }
    .preview-tile img {
      width: 100%; height: 100%; object-fit: cover;
      display: block;
    }

    .vibe-slider {
      width: 100%;
      accent-color: var(--accent);
    }

    .feed-screen {
      position: relative;
      min-height: 560px;
      background: linear-gradient(180deg, #251738, #101522);
      color: white;
      border-radius: 24px;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,0.08);
    }
    .feed-image {
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(0,0,0,0.32)), radial-gradient(circle at 65% 20%, rgba(255,111,181,0.28), transparent 28%), linear-gradient(180deg, #463764, #182033);
    }
    .feed-copy {
      position: absolute;
      left: 14px;
      right: 70px;
      bottom: 16px;
      z-index: 2;
    }
    .feed-copy h4 { margin: 0 0 6px; font-size: 17px; }
    .feed-copy p { margin: 0; font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.86); }
    .feed-actions {
      position: absolute;
      right: 12px;
      bottom: 18px;
      z-index: 2;
      display: grid;
      gap: 10px;
      justify-items: center;
    }
    .fab {
      width: 46px; height: 46px;
      border-radius: 16px;
      border: none;
      background: rgba(255,255,255,0.16);
      color: white;
      backdrop-filter: blur(10px);
      cursor: pointer;
      font-size: 16px;
    }

    .profile-head {
      display: grid;
      grid-template-columns: 64px 1fr auto;
      gap: 12px;
      align-items: center;
    }
    .avatar {
      width: 64px; height: 64px; border-radius: 20px;
      background: linear-gradient(135deg, #dcdffd, #f3d9ff);
      display: grid; place-items: center;
      font-weight: 700;
      color: #3b3fad;
      overflow: hidden;
    }
    .avatar img { width: 100%; height: 100%; object-fit: cover; }
    .profile-name { font-size: 18px; font-weight: 700; letter-spacing: -0.02em; }
    .profile-mail { font-size: 12px; color: var(--muted); margin-top: 4px; }

    .wardrobe-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }
    .wardrobe-item {
      border-radius: 18px;
      border: 1px solid var(--line);
      overflow: hidden;
      background: rgba(255,255,255,0.88);
    }
    .wardrobe-thumb {
      height: 118px;
      background: linear-gradient(180deg, #eadfd6, #d7c2b1);
      position: relative;
    }
    .wardrobe-thumb.alt { background: linear-gradient(180deg, #d9e2ff, #c4d4ff); }
    .wardrobe-meta { padding: 10px; }
    .wardrobe-meta b { display:block; font-size: 13px; margin-bottom: 3px; }
    .wardrobe-meta span { font-size: 12px; color: var(--muted); }

    .loading-box {
      display: grid;
      gap: 12px;
      align-items: center;
    }
    .loading-line {
      height: 10px;
      border-radius: 999px;
      overflow: hidden;
      background: rgba(17,24,39,0.08);
    }
    .loading-line > div {
      height: 100%;
      width: 38%;
      background: linear-gradient(90deg, var(--accent), var(--accent-2));
      animation: loadMove 2.4s ease-in-out infinite;
    }
    @keyframes loadMove {
      0% { transform: translateX(-10%); width: 26%; }
      50% { transform: translateX(170%); width: 42%; }
      100% { transform: translateX(-10%); width: 26%; }
    }

    .bottom-nav {
      margin-top: auto;
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 6px;
      padding: 10px 10px 14px;
      border-top: 1px solid rgba(229,231,235,0.8);
      background: rgba(255,255,255,0.82);
      backdrop-filter: blur(12px);
      flex-shrink: 0;
    }
    .tab {
      border: none;
      background: transparent;
      border-radius: 16px;
      padding: 10px 6px;
      display: grid;
      justify-items: center;
      gap: 5px;
      cursor: pointer;
      color: var(--muted);
      font-size: 11px;
      font-weight: 600;
      transition: background .16s ease, color .16s ease;
    }
    .tab .ico {
      width: 32px; height: 32px; border-radius: 12px;
      display:grid; place-items:center;
      background: transparent;
      transition: all .18s ease;
      font-size: 14px;
    }
    .tab.active { color: var(--accent); }
    .tab.active .ico { background: var(--accent-soft); }

    .notes {
      margin-top: 26px;
      display: grid;
      grid-template-columns: repeat(3, minmax(0,1fr));
      gap: 16px;
    }
    .notes ul { margin: 0; padding-left: 18px; color: var(--muted); font-size: 13px; line-height: 1.7; }

    @media (max-width: 1480px) {
      .phone-stage { grid-template-columns: repeat(3, minmax(250px, 1fr)); }
    }
    @media (max-width: 1180px) {
      .overview, .notes { grid-template-columns: 1fr; }
      .phone-stage { grid-template-columns: repeat(2, minmax(260px, 1fr)); }
    }
    @media (max-width: 760px) {
      .page { padding: 18px; }
      .headline h1 { font-size: 28px; }
      .phone-stage { grid-template-columns: 1fr; }
      .phone { max-width: 100%; }
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="topbar">
      <div class="headline">
        <h1>fitme.ai next-gen mobile concept</h1>
        <p>
          Updated interactive redesign with your added ideas embedded: real camera/upload entry points, vibe-based try-on,
          TikTok-style feed behavior, wardrobe/profile storage, download actions, fun loading language, and a more polished
          Gen Z fashion-tech finish that still keeps the architecture practical.
        </p>
      </div>
      <div class="controls">
        <button class="pill-btn active" data-theme="violet">Violet Luxe</button>
        <button class="pill-btn" data-theme="mint">Mint Pop</button>
        <button class="pill-btn" data-theme="midnight">Midnight Glow</button>
        <button class="seg-btn active" data-density="comfortable">Comfortable Type</button>
        <button class="seg-btn" data-density="compact">Compact Type</button>
      </div>
    </div>

    <div class="overview">
      <div class="card">
        <h2>Key upgrades embedded into this concept</h2>
        <div class="upgrade-list">
          <div class="upgrade"><div class="badge-icon">📸</div><div><b>Real camera + upload paths</b><span>The try-on entry screen now includes a live camera area, upload affordances, and device-first image previews.</span></div></div>
          <div class="upgrade"><div class="badge-icon">✨</div><div><b>Image-to-image try-on story</b><span>The result flow assumes user photo + garment + vibe scene selection, instead of prompt-only image generation.</span></div></div>
          <div class="upgrade"><div class="badge-icon">👗</div><div><b>Wardrobe profile</b><span>Saved fits and liked garments now live inside Profile as a proper wardrobe grid.</span></div></div>
          <div class="upgrade"><div class="badge-icon">📱</div><div><b>Swipe feed behavior</b><span>The discovery surface is reimagined as a vertical fashion feed with floating actions for quick try-on.</span></div></div>
          <div class="upgrade"><div class="badge-icon">💅</div><div><b>Polish details</b><span>Download action, tactile press feedback, dynamic accent themes, and rotating loading quotes all make the experience feel more alive.</span></div></div>
        </div>
      </div>
      <div class="card">
        <h2>What these changes solve</h2>
        <div class="issue-list">
          <div class="issue"><div class="badge-icon">1</div><div><b>No clear mobile navigation</b><span>Persistent bottom tabs now anchor the app: Feed, Try-On, Saved, Shops, Profile.</span></div></div>
          <div class="issue"><div class="badge-icon">2</div><div><b>Prototype felt static</b><span>Camera, upload, vibe, loading, save, and download states make it feel like a real product journey.</span></div></div>
          <div class="issue"><div class="badge-icon">3</div><div><b>Profile felt incomplete</b><span>Wardrobe, likes, measurements, style presets, and account actions are now centered in profile.</span></div></div>
          <div class="issue"><div class="badge-icon">4</div><div><b>Gen Z aesthetic needed more personality</b><span>Feed-first discovery, expressive accents, and scene vibes add cultural relevance without losing clarity.</span></div></div>
        </div>
      </div>
    </div>

    <div class="phone-stage">
      <div class="phone-wrap">
        <div class="phone-title"><span>For You / swipe feed</span><span>Reels-style discovery</span></div>
        <div class="phone">
          <div class="phone-inner">
            <div class="statusbar"><span>9:41</span><span>5G • 92%</span></div>
            <div class="appbar">
              <div>
                <div class="eyebrow">fitme.ai</div>
                <div class="title">For you</div>
                <div class="subtitle">Swipe through mood-led looks and jump into try-on instantly.</div>
              </div>
              <button class="circle-btn">⌕</button>
            </div>
            <div class="content">
              <div class="feed-screen">
                <div class="feed-image"></div>
                <div class="feed-copy">
                  <div class="chip-row" style="margin-bottom:8px;">
                    <span class="chip dark">Cyber core</span>
                    <span class="chip dark">₹1499</span>
                  </div>
                  <h4>Mesh-layer street look</h4>
                  <p>Low-rise denim, cropped bomber, chrome accessories. Swipe up for the next fit or tap Try On.</p>
                </div>
                <div class="feed-actions">
                  <button class="fab">♡</button>
                  <button class="fab">↗</button>
                  <button class="fab" id="jumpTryOn">✨</button>
                </div>
              </div>
              <div class="panel">
                <h4>Why this works better</h4>
                <p>A vertical feed makes browsing faster and more addictive than grid cards, especially for trend-driven audiences.</p>
              </div>
            </div>
            <div class="bottom-nav" data-tabset="feed">
              <button class="tab active"><span class="ico">⌂</span><span>Feed</span></button>
              <button class="tab"><span class="ico">◌</span><span>Try-On</span></button>
              <button class="tab"><span class="ico">♡</span><span>Saved</span></button>
              <button class="tab"><span class="ico">⌖</span><span>Shops</span></button>
              <button class="tab"><span class="ico">☻</span><span>Profile</span></button>
            </div>
          </div>
        </div>
      </div>

      <div class="phone-wrap">
        <div class="phone-title"><span>Camera + upload</span><span>real entry points</span></div>
        <div class="phone">
          <div class="phone-inner">
            <div class="statusbar"><span>9:41</span><span>Wi‑Fi • 92%</span></div>
            <div class="appbar">
              <div>
                <div class="eyebrow">Try-On</div>
                <div class="title">Capture your fit</div>
                <div class="subtitle">Use live camera, upload from gallery, then pick the scene vibe before generating.</div>
              </div>
              <button class="circle-btn" id="toggleCamera">◎</button>
            </div>
            <div class="content">
              <div class="camera-box">
                <video id="camera" autoplay muted playsinline></video>
                <div class="camera-overlay"><div class="frame"></div></div>
              </div>
              <div class="camera-actions">
                <button class="btn secondary" id="startCamBtn">Start camera</button>
                <button class="btn secondary" id="captureBtn">Capture</button>
                <button class="btn primary" id="uploadBtn">Upload photo</button>
                <input type="file" id="fileInput" accept="image/*" hidden />
              </div>
              <div class="preview-grid">
                <div class="preview-tile" id="userPreview">Your photo</div>
                <div class="preview-tile" id="garmentPreview">Garment image</div>
              </div>
              <div class="panel">
                <h4>Scene vibe</h4>
                <p id="vibeLabel">Cyberpunk City</p>
                <input class="vibe-slider" id="vibeSlider" type="range" min="0" max="2" value="0" />
                <div class="theme-row" style="margin-top:10px;">
                  <span class="chip tonal">Cyberpunk City</span>
                  <span class="chip">Cozy Coffee Shop</span>
                  <span class="chip">Y2K Studio</span>
                </div>
              </div>
              <div class="cta-row">
                <button class="btn primary" id="generateBtn">Generate try-on</button>
                <button class="btn secondary">Scan product</button>
              </div>
            </div>
            <div class="bottom-nav" data-tabset="camera">
              <button class="tab"><span class="ico">⌂</span><span>Feed</span></button>
              <button class="tab active"><span class="ico">◌</span><span>Try-On</span></button>
              <button class="tab"><span class="ico">♡</span><span>Saved</span></button>
              <button class="tab"><span class="ico">⌖</span><span>Shops</span></button>
              <button class="tab"><span class="ico">☻</span><span>Profile</span></button>
            </div>
          </div>
        </div>
      </div>

      <div class="phone-wrap">
        <div class="phone-title"><span>AI loading / result</span><span>fun + useful</span></div>
        <div class="phone">
          <div class="phone-inner">
            <div class="statusbar"><span>9:41</span><span>5G • 92%</span></div>
            <div class="appbar">
              <div>
                <div class="eyebrow">Generation</div>
                <div class="title">Your look is cooking</div>
                <div class="subtitle">Better loading personality, then a result surface with save and download actions.</div>
              </div>
              <button class="circle-btn">⇩</button>
            </div>
            <div class="content">
              <div class="panel loading-box">
                <div class="loading-line"><div></div></div>
                <h4 id="loadingQuote">Consulting the fashion gods...</h4>
                <p>We are blending your portrait, garment silhouette, vibe, and fit profile into a shareable result.</p>
              </div>
              <div class="preview-grid">
                <div class="preview-tile">Original</div>
                <div class="preview-tile"><img id="resultImg" alt="Generated preview" /></div>
              </div>
              <div class="chip-row">
                <span class="chip success">Saved-fit ready</span>
                <span class="chip tonal">Scene: <span id="sceneTag">Cyberpunk</span></span>
                <span class="chip">Downloadable</span>
              </div>
              <div class="panel">
                <h4>AI result actions</h4>
                <p>Turn a generated image into a wardrobe item, shareable social asset, or downloadable phone image.</p>
              </div>
              <div class="cta-row">
                <button class="btn primary" id="saveWardrobeBtn">Save to wardrobe</button>
                <button class="btn secondary" id="downloadBtn">Download</button>
              </div>
            </div>
            <div class="bottom-nav" data-tabset="result">
              <button class="tab"><span class="ico">⌂</span><span>Feed</span></button>
              <button class="tab active"><span class="ico">◌</span><span>Try-On</span></button>
              <button class="tab"><span class="ico">♡</span><span>Saved</span></button>
              <button class="tab"><span class="ico">⌖</span><span>Shops</span></button>
              <button class="tab"><span class="ico">☻</span><span>Profile</span></button>
            </div>
          </div>
        </div>
      </div>

      <div class="phone-wrap">
        <div class="phone-title"><span>Wardrobe / profile</span><span>saved fits live here</span></div>
        <div class="phone">
          <div class="phone-inner">
            <div class="statusbar"><span>9:41</span><span>5G • 92%</span></div>
            <div class="appbar">
              <div>
                <div class="eyebrow">Profile</div>
                <div class="title">Your wardrobe</div>
                <div class="subtitle">Saved AI looks, liked garments, and style identity all in one place.</div>
              </div>
              <button class="circle-btn">⎋</button>
            </div>
            <div class="content">
              <div class="panel profile-head">
                <div class="avatar">AI</div>
                <div>
                  <div class="profile-name">fitme.ai user</div>
                  <div class="profile-mail">student@campus.com</div>
                  <div class="action-row" style="margin-top:8px;">
                    <span class="chip success">6 saved fits</span>
                    <span class="chip">Y2K • Streetwear</span>
                  </div>
                </div>
                <button class="btn secondary" style="padding:10px 12px;">Logout</button>
              </div>
              <div class="wardrobe-grid" id="wardrobeGrid">
                <div class="wardrobe-item"><div class="wardrobe-thumb"></div><div class="wardrobe-meta"><b>Cyber jacket fit</b><span>Saved just now</span></div></div>
                <div class="wardrobe-item"><div class="wardrobe-thumb alt"></div><div class="wardrobe-meta"><b>Studio denim look</b><span>Liked garment</span></div></div>
                <div class="wardrobe-item"><div class="wardrobe-thumb alt"></div><div class="wardrobe-meta"><b>Coffee date fit</b><span>Budget pick</span></div></div>
                <div class="wardrobe-item"><div class="wardrobe-thumb"></div><div class="wardrobe-meta"><b>Campus formal</b><span>Interview board</span></div></div>
              </div>
              <div class="panel">
                <h4>Profile controls</h4>
                <p>Measurements, style presets, liked pieces, and wardrobe collections should all branch from this hub.</p>
              </div>
            </div>
            <div class="bottom-nav" data-tabset="profile">
              <button class="tab"><span class="ico">⌂</span><span>Feed</span></button>
              <button class="tab"><span class="ico">◌</span><span>Try-On</span></button>
              <button class="tab"><span class="ico">♡</span><span>Saved</span></button>
              <button class="tab"><span class="ico">⌖</span><span>Shops</span></button>
              <button class="tab active"><span class="ico">☻</span><span>Profile</span></button>
            </div>
          </div>
        </div>
      </div>

      <div class="phone-wrap">
        <div class="phone-title"><span>Offers / shopping</span><span>buy from vibe result</span></div>
        <div class="phone">
          <div class="phone-inner">
            <div class="statusbar"><span>9:41</span><span>5G • 92%</span></div>
            <div class="appbar">
              <div>
                <div class="eyebrow">Shop</div>
                <div class="title">Complete the look</div>
                <div class="subtitle">Translate the generated vibe directly into comparable products and offers.</div>
              </div>
              <button class="circle-btn">☰</button>
            </div>
            <div class="content">
              <div class="hero">
                <h3>Scene-matched picks</h3>
                <p>Products are ranked by fit, vibe, and budget so the visual result becomes shoppable immediately.</p>
              </div>
              <div class="panel product">
                <div class="product-img"></div>
                <div>
                  <h4>Vinyl cropped jacket</h4>
                  <p>Strong match for Cyberpunk City. Similar silhouette to your saved result with better price this week.</p>
                  <div class="action-row" style="margin:10px 0 8px;">
                    <span class="chip success">Best vibe</span>
                    <span class="chip">₹1,799</span>
                    <span class="chip tonal">2 offers</span>
                  </div>
                  <div class="price">₹1,799 <span class="small">Myntra · Ajio</span></div>
                </div>
              </div>
              <div class="panel">
                <h4>Bundle suggestion</h4>
                <p>Add silver hoops and platform boots to push the result closer to the generated scene aesthetic.</p>
              </div>
              <div class="cta-row">
                <button class="btn primary">Open retailer</button>
                <button class="btn secondary">Save products</button>
              </div>
            </div>
            <div class="bottom-nav" data-tabset="shop">
              <button class="tab"><span class="ico">⌂</span><span>Feed</span></button>
              <button class="tab"><span class="ico">◌</span><span>Try-On</span></button>
              <button class="tab"><span class="ico">♡</span><span>Saved</span></button>
              <button class="tab active"><span class="ico">⌖</span><span>Shops</span></button>
              <button class="tab"><span class="ico">☻</span><span>Profile</span></button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="notes">
      <div class="card">
        <h3>Screens added from your suggestions</h3>
        <ul>
          <li>real camera/upload try-on entry</li>
          <li>vibe-aware generation controls</li>
          <li>result screen with save and download</li>
          <li>wardrobe-style profile screen</li>
          <li>TikTok-style vertical discovery feed</li>
        </ul>
      </div>
      <div class="card">
        <h3>Production-grade direction</h3>
        <ul>
          <li>use image-to-image provider architecture instead of prompt-only generation</li>
          <li>keep wardrobe in local state first, then back it with API persistence</li>
          <li>treat scene vibes as recommendation metadata, not just cosmetic labels</li>
          <li>keep bottom navigation persistent across all high-traffic screens</li>
        </ul>
      </div>
      <div class="card">
        <h3>What this prototype is best for</h3>
        <ul>
          <li>design handoff to Codex</li>
          <li>mobile UX direction review</li>
          <li>team discussion around feed, try-on, wardrobe, and shopping flow</li>
          <li>interactive proof before translating into Expo screens</li>
        </ul>
      </div>
    </div>
  </div>

  <script>
    const themes = {
      violet: {
        '--bg': '#f6f7fb', '--text': '#151823', '--muted': '#6b7280', '--line': '#e5e7eb', '--accent': '#6d5efc', '--accent-soft': 'rgba(109,94,252,0.12)', '--accent-2': '#ff6fb5', '--page-glow': 'radial-gradient(circle at top left, rgba(109,94,252,0.10), transparent 28%), radial-gradient(circle at bottom right, rgba(255,111,181,0.08), transparent 24%)'
      },
      mint: {
        '--bg': '#f4fbf8', '--text': '#13211d', '--muted': '#60706b', '--line': '#dfe9e5', '--accent': '#0ea67f', '--accent-soft': 'rgba(14,166,127,0.12)', '--accent-2': '#4fd1c5', '--page-glow': 'radial-gradient(circle at top left, rgba(14,166,127,0.10), transparent 28%), radial-gradient(circle at bottom right, rgba(79,209,197,0.08), transparent 24%)'
      },
      midnight: {
        '--bg': '#f5f6fb', '--text': '#121625', '--muted': '#636b80', '--line': '#e2e5f1', '--accent': '#3245d1', '--accent-soft': 'rgba(50,69,209,0.12)', '--accent-2': '#8c52ff', '--page-glow': 'radial-gradient(circle at top left, rgba(50,69,209,0.10), transparent 28%), radial-gradient(circle at bottom right, rgba(140,82,255,0.08), transparent 24%)'
      }
    };

    document.querySelectorAll('[data-theme]').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('[data-theme]').forEach(x => x.classList.remove('active'));
        btn.classList.add('active');
        const theme = themes[btn.dataset.theme];
        Object.entries(theme).forEach(([key, value]) => document.documentElement.style.setProperty(key, value));
      });
    });

    document.querySelectorAll('[data-density]').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('[data-density]').forEach(x => x.classList.remove('active'));
        btn.classList.add('active');
        const compact = btn.dataset.density === 'compact';
        document.documentElement.style.setProperty('--phone-h', compact ? '750px' : '790px');
        document.querySelectorAll('.title').forEach(el => el.style.fontSize = compact ? '20px' : '22px');
        document.querySelectorAll('.subtitle, .panel p').forEach(el => el.style.fontSize = compact ? '12px' : '13px');
      });
    });

    document.querySelectorAll('.bottom-nav .tab').forEach(tab => {
      tab.addEventListener('click', () => {
        const parent = tab.closest('.bottom-nav');
        parent.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));
        tab.classList.add('active');
      });
    });

    const vibes = ['Cyberpunk City', 'Cozy Coffee Shop', 'Y2K Studio'];
    const vibeShort = ['Cyberpunk', 'Coffee Shop', 'Y2K'];
    const vibeSlider = document.getElementById('vibeSlider');
    const vibeLabel = document.getElementById('vibeLabel');
    const sceneTag = document.getElementById('sceneTag');
    vibeSlider?.addEventListener('input', () => {
      const i = Number(vibeSlider.value);
      vibeLabel.textContent = vibes[i];
      sceneTag.textContent = vibeShort[i];
    });

    const loadingQuotes = [
      'Consulting the fashion gods...',
      'Manifesting the drip...',
      'Serving pixels, not crumbs...',
      'Aligning your aura with the outfit...',
      'Rendering main-character energy...'
    ];
    let quoteIndex = 0;
    const loadingQuote = document.getElementById('loadingQuote');
    setInterval(() => {
      if (!loadingQuote) return;
      quoteIndex = (quoteIndex + 1) % loadingQuotes.length;
      loadingQuote.textContent = loadingQuotes[quoteIndex];
    }, 2200);

    const video = document.getElementById('camera');
    const startCamBtn = document.getElementById('startCamBtn');
    const toggleCamera = document.getElementById('toggleCamera');
    let currentStream = null;

    async function startCamera() {
      if (!navigator.mediaDevices?.getUserMedia) return;
      try {
        currentStream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: 'user' }, audio: false });
        video.srcObject = currentStream;
      } catch (err) {
        console.warn('Camera unavailable in this environment', err);
      }
    }

    startCamBtn?.addEventListener('click', startCamera);
    toggleCamera?.addEventListener('click', startCamera);

    const userPreview = document.getElementById('userPreview');
    const garmentPreview = document.getElementById('garmentPreview');
    const fileInput = document.getElementById('fileInput');
    const uploadBtn = document.getElementById('uploadBtn');
    const captureBtn = document.getElementById('captureBtn');
    const resultImg = document.getElementById('resultImg');

    function setPreview(target, src) {
      target.innerHTML = '';
      const img = document.createElement('img');
      img.src = src;
      target.appendChild(img);
    }

    uploadBtn?.addEventListener('click', () => fileInput?.click());
    fileInput?.addEventListener('change', e => {
      const file = e.target.files?.[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        setPreview(userPreview, reader.result);
        setPreview(garmentPreview, reader.result);
        resultImg.src = reader.result;
      };
      reader.readAsDataURL(file);
    });

    captureBtn?.addEventListener('click', () => {
      if (!video?.srcObject) return;
      const canvas = document.createElement('canvas');
      canvas.width = video.videoWidth || 360;
      canvas.height = video.videoHeight || 480;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
      const data = canvas.toDataURL('image/png');
      setPreview(userPreview, data);
      resultImg.src = data;
    });

    document.getElementById('generateBtn')?.addEventListener('click', () => {
      document.getElementById('loadingQuote').textContent = 'Manifesting the drip...';
      if (!resultImg.src && userPreview.querySelector('img')) {
        resultImg.src = userPreview.querySelector('img').src;
      }
    });

    document.getElementById('saveWardrobeBtn')?.addEventListener('click', () => {
      const grid = document.getElementById('wardrobeGrid');
      const src = resultImg.src || userPreview.querySelector('img')?.src;
      if (!grid || !src) return;
      const item = document.createElement('div');
      item.className = 'wardrobe-item';
      item.innerHTML = `<div class="wardrobe-thumb"><img src="${src}" alt="Saved wardrobe look" style="width:100%;height:100%;object-fit:cover;" /></div><div class="wardrobe-meta"><b>New saved fit</b><span>Just added from result</span></div>`;
      grid.prepend(item);
    });

    document.getElementById('downloadBtn')?.addEventListener('click', () => {
      const src = resultImg.src || userPreview.querySelector('img')?.src;
      if (!src) return;
      const link = document.createElement('a');
      link.href = src;
      link.download = 'fitme-look.png';
      link.click();
    });

    document.getElementById('jumpTryOn')?.addEventListener('click', () => {
      document.querySelectorAll('.bottom-nav').forEach(nav => {
        const tabs = nav.querySelectorAll('.tab');
        tabs.forEach(t => t.classList.remove('active'));
        if (tabs[1]) tabs[1].classList.add('active');
      });
      window.scrollTo({ top: document.querySelectorAll('.phone-wrap')[1].offsetTop - 40, behavior: 'smooth' });
    });
  </script>
</body>
</html>
