{
  "name": "@fitme/config",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "react-native": "src/index.ts",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('No tests configured for @fitme/config yet')\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
