name: Android Preview Build

on:
  push:
    branches:
      - main

jobs:
  android_preview:
    name: Build Android APK
    type: build
    params:
      platform: android
      profile: preview
      projectRootDirectory: apps/mobile
