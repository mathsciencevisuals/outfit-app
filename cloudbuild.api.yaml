substitutions:
  _REGION: europe-west4
  _AR_REPOSITORY: outfit-app
  _SERVICE_NAME: fitme-api
  _IMAGE_NAME: api
  _PLATFORM: managed
  _SERVICE_ACCOUNT: fitme-api-runner@${PROJECT_ID}.iam.gserviceaccount.com
  _CLOUDSQL_INSTANCE: ""
  _VPC_NETWORK: default
  _VPC_SUBNET: default
  _REDIS_URL: redis://10.0.0.3:6379
  _STORAGE_PROVIDER: gcs
  _GCS_BUCKET: fitme-assets
  _GCS_PUBLIC_BASE_URL: https://storage.googleapis.com/fitme-assets
  _TRYON_PROVIDER: mock

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/api/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPOSITORY}/${_IMAGE_NAME}:${COMMIT_SHA}
      - .
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPOSITORY}/${_IMAGE_NAME}:${COMMIT_SHA}
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --platform=${_PLATFORM}
      - --region=${_REGION}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPOSITORY}/${_IMAGE_NAME}:${COMMIT_SHA}
      - --service-account=${_SERVICE_ACCOUNT}
      - --allow-unauthenticated
      - --port=8080
      - --cpu=1
      - --memory=1Gi
      - --min-instances=0
      - --max-instances=10
      - --add-cloudsql-instances=${_CLOUDSQL_INSTANCE}
      - --network=${_VPC_NETWORK}
      - --subnet=${_VPC_SUBNET}
      - --vpc-egress=private-ranges-only
      - --set-env-vars=NODE_ENV=production,STORAGE_PROVIDER=${_STORAGE_PROVIDER},GCS_BUCKET=${_GCS_BUCKET},GCS_PROJECT_ID=${PROJECT_ID},GCS_PUBLIC_BASE_URL=${_GCS_PUBLIC_BASE_URL},REDIS_URL=${_REDIS_URL},TRYON_PROVIDER=${_TRYON_PROVIDER}
      - --update-secrets=DATABASE_URL=fitme-database-url:latest,JWT_SECRET=fitme-jwt-secret:latest

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPOSITORY}/${_IMAGE_NAME}:${COMMIT_SHA}
